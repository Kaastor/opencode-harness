# Tiny Assignment

Fix `math.js` so the deterministic check passes.

Rules:

- Change only the starter workspace files needed for the fix.
- Do not modify the check script.
- Keep the solution minimal.
